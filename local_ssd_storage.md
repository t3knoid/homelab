# Local SSD storage

Local storage is a mounted folder in each node's first SSD local drive. The folder is located in /var/lib/vz and is identified with the "local" id. It is configured to hold Disk image, Container, Container Template, ISO image, and Snippets.